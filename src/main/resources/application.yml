server:
  port: 8881

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: git@github.com-khagesh:khagesh-java/microservices-config.git
          default-label: main